{
  "name": "backend-home-assignment-junior",
  "version": "1.0.0",
  "description": "## Requirements Use the file `data.csv` as the data source.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "binary-search-tree": "^0.2.6",
    "csv-parser": "^3.0.0",
    "express": "^4.19.2",
    "trie-search": "^2.0.0"
  }
}
